/** operator artimatika
* operator  = keterangan
* + = pertambahan
* -, -expression = pengurangan atau negatif
* * = perkalian
* / = pebagian, hasil double
* ~/ = pembagian, hasil int
* % = sisa bagi
**/

/** operator penugasan
 * a += 10;
 * a -= 10;
 * a *= 10;
 * a /= 10;
 * a ~/= 10;
 * a %= 10;
 */

/** operator increament and decrement
 * operator = keterangan 
 * ++var = var = var + 1 (expresion is var + 1)
 * var++ = var = var + 1 (expresion is var)
 * --var = var = var - 1 (expresion is var + 1)
 * var-- = var - var - 1 (expresion is var)
 */

/** operator logika
 *  &&  = dua-duanya harus benar
 *  || = salah satu harus benar
 *  !  = kebalikan
 *  != 
 *  <=
 *  >=
 *  <
 *  >
 */

/** operator type test
 * oprator = keterangan
 * as = typecast, melakukan konversi tipe data secara paksa
 * is = true, jika object sesuai tipe data
 * is! = true, jika object tidak sesuai tipe data
 */